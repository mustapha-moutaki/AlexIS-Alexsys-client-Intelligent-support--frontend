export default function CreateClientForm({onSubmit, isLoading}:{
    onSubmit: (data: any) => void;
    isLoading: boolean;
}) {
    return (
        <div>
            <h1>Create Client Form</h1>
        </div>
    );
}