export interface User {
    id: string | number;
    username?: string;
    email: string;
    firstName?: string;
    lastName?: string;
    role?: string;
    profilePicture?: string;
    phoneNumber?: string;
}